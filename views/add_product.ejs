<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>Dashboard</title>
</head>
<body>	
			<% if (typeof (error) != 'undefined') { %>

				<% error.forEach(function(error){ %>

							<h5 style="color: red;"><%= error.msg %></h5>

				<% }) %>

			<% } %>

			<% if(message != '') {%>
				<h6 style="color:green"><%= message %></h6>

			<% } %>

			<% if(error_msg != '') { %>

				<h6 style="color:red"><%= error_msg %></h6>

			<% } %>

		<h3>Welcome!!!</h3>

		<p>Logged in Merchant: <strong><%= username %></strong></p>
		<hr>

		<a href="/dashboard">Home</a>
		<a href="/addPro">Upload Products</a>
		<a href="/viewPro">View Products</a>
		<a href="/logout">Click to Logout</a>

		<hr>

		<h3>Product Upload Page</h3>
		<p>Please fill in the product details below</p>

		<form action="/addPro" method="POST">
			<p>Product Name: <input type="text" name="productName"></p>
			<p>Product Description: <br><textarea name="description" rows="7" cols="25"></textarea></p>
			<p>Product Price: <input type="number" name="price"></p>

			<% let products = ["Computers", "Phones", "Tablets", "Printers", "Accessories"]%>

			<p>Product Category: <select name="category">
				<option value="">Select Product Category</option>
			<% products.forEach((prods) => { %>
				<option value="<%= prods %>"><%= prods %></option>

			<% }) %>
				
			</select></p>

			<button type="submit">Click to Add</button>
			
		</form>

</body>
</html>